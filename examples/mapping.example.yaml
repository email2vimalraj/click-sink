# Generated by `click-sink detect` and editable by users.
columns:
  - fieldPath: "user.id"
    column: "user_id"
    type: "Int64"           # or Nullable(Int64)
    nullable: false
  - fieldPath: "user.email"
    column: "user_email"
    type: "String"
    nullable: true
  - fieldPath: "event.ts"
    column: "event_ts"
    type: "String"         # In future could be DateTime64
    nullable: false
